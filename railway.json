{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cd unimate-website && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd unimate-website && npm start",
    "healthcheckPath": "/"
  }
}
